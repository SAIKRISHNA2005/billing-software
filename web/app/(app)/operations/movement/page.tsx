import ComingSoon from '@/components/common/ComingSoon';

export default function VehicleMovementPage() {
  return (
    <ComingSoon
      title="Vehicle Movement"
      phase={8}
      phaseName="Operations"
      description="Daily control room table with inline quick-editing for factory, print gate, and seaport terminal gate-in/out timestamps."
    />
  );
}
