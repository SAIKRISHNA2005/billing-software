import ComingSoon from '@/components/common/ComingSoon';

export default function VendorReportPage() {
  return (
    <ComingSoon
      title="Vendor Report"
      phase={10}
      phaseName="Vendors"
      description="Consolidated vendor report summarizing vehicle counts, total trips, diesel advances, halting amounts, total payable, paid amounts, and net pending balance."
    />
  );
}
