import ComingSoon from '@/components/common/ComingSoon';

export default function CompanyProfileSettingsPage() {
  return (
    <ComingSoon
      title="Company Profile & Numbering Settings"
      phase={11}
      phaseName="Settings"
      description="Update company legal details, address, tax identifiers (GSTIN, PAN), upload company seal and authorized signature images to Google Drive, and configure numbering starts."
    />
  );
}
