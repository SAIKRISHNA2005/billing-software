import ComingSoon from '@/components/common/ComingSoon';

export default function EnquiriesListPage() {
  return (
    <ComingSoon
      title="View / Edit Enquiries"
      phase={7}
      phaseName="Enquiry Frontend"
      description="Interactive data table for transport enquiries with multi-criteria filtering by company, client, stage, vendor, and date range, plus stage stepper and history."
    />
  );
}
