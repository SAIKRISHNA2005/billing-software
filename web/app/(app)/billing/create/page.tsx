import ComingSoon from '@/components/common/ComingSoon';

export default function CreateBillPage() {
  return (
    <ComingSoon
      title="Create Bill"
      phase={13}
      phaseName="Billing Frontend"
      description="Invoice builder with pre-filled line items (Transportation Charges, Halting Charges), live total calculation, and atomic sequential bill numbering."
    />
  );
}
