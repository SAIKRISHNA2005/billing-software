import ComingSoon from '@/components/common/ComingSoon';

export default function ReportsVendorPage() {
  return (
    <ComingSoon
      title="Vendor Settlement Report"
      phase={10}
      phaseName="Vendors"
      description="Vendor performance, trip settlements, advances, diesel charges, and outstanding balances across all transport partners."
    />
  );
}
