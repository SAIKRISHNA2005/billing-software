import ComingSoon from '@/components/common/ComingSoon';

export default function BillingReportPage() {
  return (
    <ComingSoon
      title="Billing & Receivables Report"
      phase={16}
      phaseName="Reports"
      description="Invoice summary listing all processed bills, amounts billed, payments collected, outstanding client balances, and payment status flags."
    />
  );
}
